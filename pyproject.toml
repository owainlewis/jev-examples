[project]
name = "jev-examples"
version = "0.1.0"
description = "Python examples and tutorials for building with Jev"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typesafe-sdk==0.7.0",
    "python-dotenv==1.2.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
