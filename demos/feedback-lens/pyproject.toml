[project]
name = "jev-feedback-lens"
version = "0.1.0"
description = "A local workbench for Jev's Choice, Noul, and Score"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.135.1",
  "uvicorn==0.41.0",
  "typesafe-sdk==0.7.0",
  "python-dotenv==1.2.1",
]

[dependency-groups]
dev = ["httpx==0.28.1", "ruff==0.15.6"]
